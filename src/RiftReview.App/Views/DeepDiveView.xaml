<UserControl x:Class="RiftReview.App.Views.DeepDiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:RiftReview.App.Controls">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource PanelBgBrush}">
        <!-- Empty-state overlay: shown when no match selected -->
        <TextBlock Text="Select a match to see the deep-dive…"
                   Foreground="{StaticResource TextMutedBrush}"
                   FontSize="14"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasData}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Data content: shown only when HasData=true -->
        <Grid Visibility="{Binding HasData, Converter={StaticResource BoolToVis}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource HairlineBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,8">
                <TextBlock Text="{Binding Header}"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           FontSize="15"
                           FontWeight="SemiBold"/>
            </Border>

            <!-- Swing & causality (M8) -->
            <Border Grid.Row="1"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource HairlineBrush}"
                    BorderThickness="0,0,0,1"
                    Margin="0,0,0,2"
                    Padding="12,8"
                    Visibility="{Binding HasCausality, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <TextBlock Text="Swing &amp; causality"
                               Foreground="{StaticResource AccentBrush}"
                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>

                    <!-- Item 13: where the game turned -->
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="Where the game turned: " Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding SwingText}" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>

                    <!-- Item 14: deaths in context -->
                    <ItemsControl ItemsSource="{Binding DeathContexts}" Margin="0,6,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,1,0,1">
                                    <TextBlock Text="Died " Foreground="{StaticResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding Minute}" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock Text=" — " Foreground="{StaticResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding Gold}" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <!-- Item 15: back-timing -->
                    <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                        <TextBlock Text="Backs: " Foreground="{StaticResource TextMutedBrush}"/>
                        <TextBlock Text="{Binding BackSummary}" Foreground="{StaticResource TextPrimaryBrush}"/>
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding Backs}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Text}" Margin="0,0,10,0" Opacity="0.85"
                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <TextBlock Text="{Binding TurningPointLag}" Margin="0,4,0,0"
                               Foreground="{StaticResource TextMutedBrush}"
                               Visibility="{Binding HasLag, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </Border>

            <!-- Vision & objectives -->
            <Border Grid.Row="2"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource HairlineBrush}"
                    BorderThickness="0,0,0,1"
                    Margin="0,0,0,2"
                    Padding="12,8">
                <StackPanel>
                    <TextBlock Text="Vision &amp; objectives"
                               Foreground="{StaticResource AccentBrush}"
                               FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <StackPanel Margin="0,0,28,0">
                            <TextBlock Text="Wards placed" Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                            <TextBlock Text="{Binding Vision.WardsPlaced}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,28,0">
                            <TextBlock Text="Wards cleared" Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                            <TextBlock Text="{Binding Vision.WardsCleared}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,28,0">
                            <TextBlock Text="Control wards" Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                            <TextBlock Text="{Binding Vision.ControlWardsPlaced}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,28,0">
                            <TextBlock Text="Vision proxy" Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                            <TextBlock Text="{Binding Vision.VisionProxy}" Foreground="{StaticResource AccentBrush}" FontSize="18"/>
                        </StackPanel>
                    </StackPanel>
                    <TextBlock Text="Vision proxy = wards placed + cleared (control wards count twice). Estimate from timeline events — not Riot's official Vision Score."
                               Foreground="{StaticResource TextMutedBrush}" FontSize="10" TextWrapping="Wrap" Margin="0,0,0,6"/>
                    <ItemsControl ItemsSource="{Binding Objectives}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Label}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Detail}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                                    <TextBlock Grid.Column="2" Text="{Binding Percent}" Foreground="{StaticResource AccentBrush}" FontSize="12"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- By game phase (M10) -->
            <Border Grid.Row="3"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource HairlineBrush}"
                    BorderThickness="0,0,0,1"
                    Margin="0,0,0,2"
                    Padding="12,8"
                    Visibility="{Binding HasPhaseBreakdown, Converter={StaticResource BoolToVis}}">
                <StackPanel>
                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="By game phase"
                                   Foreground="{StaticResource AccentBrush}"
                                   FontSize="13" FontWeight="SemiBold"/>
                        <TextBlock Grid.Column="1" Text="vs your same-role average · raw where no baseline"
                                   Foreground="{StaticResource TextMutedBrush}" FontSize="10"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="64"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Phase"    Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                        <TextBlock Grid.Column="1" Text="Gold Δ"   Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                        <TextBlock Grid.Column="2" Text="CS/min"   Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                        <TextBlock Grid.Column="3" Text="Deaths"   Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                        <TextBlock Grid.Column="4" Text="KP"       Foreground="{StaticResource TextMutedBrush}" FontSize="11"/>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding PhaseRows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource HairlineBrush}" BorderThickness="0,1,0,0" Padding="0,4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="64"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{Binding Label}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12"/>
                                            <TextBlock Text="{Binding Range}" Foreground="{StaticResource TextMutedBrush}" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding GoldDelta}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"/>
                                            <TextBlock Text="{Binding GoldBadge}" Foreground="{Binding GoldBadgeBrush}" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{Binding CsPerMin}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"/>
                                            <TextBlock Text="{Binding CsBadge}" Foreground="{Binding CsBadgeBrush}" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="3">
                                            <TextBlock Text="{Binding Deaths}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"/>
                                            <TextBlock Text="{Binding DeathsBadge}" Foreground="{Binding DeathsBadgeBrush}" FontSize="10"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="4">
                                            <TextBlock Text="{Binding Kp}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"/>
                                            <TextBlock Text="{Binding KpBadge}" Foreground="{Binding KpBadgeBrush}" FontSize="10"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="No flavor grades — every number is your raw value with its delta vs your own same-role average; blank where you have fewer than 3 prior games."
                               Foreground="{StaticResource TextMutedBrush}" FontSize="10"
                               TextWrapping="Wrap" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <!-- Gold diff chart -->
            <Border Grid.Row="4"
                    Background="{StaticResource CardBgBrush}"
                    BorderBrush="{StaticResource HairlineBrush}"
                    BorderThickness="0,0,0,1"
                    Margin="0,0,0,2"
                    Padding="12,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               Text="Gold differential — gold = vs lane, blue = vs team; ✕ = your deaths"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="11"
                               Margin="0,0,0,4"/>
                    <controls:LineChart Grid.Row="1"
                                        Series="{Binding GoldSeries}"
                                        DeathMarkers="{Binding DeathMinutes}"
                                        ShowZeroLine="True"
                                        SwingStartMinute="{Binding SwingStartMinute}"
                                        SwingEndMinute="{Binding SwingEndMinute}"
                                        MinHeight="180"/>
                </Grid>
            </Border>

            <!-- CS/min chart -->
            <Border Grid.Row="5"
                    Background="{StaticResource CardBgBrush}"
                    Padding="12,8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               Text="CS/min — gold = you, gray dashed = same-role avg, blue dashed = rank avg"
                               Foreground="{StaticResource TextMutedBrush}"
                               FontSize="11"
                               Margin="0,0,0,4"/>
                    <controls:LineChart Grid.Row="1"
                                        Series="{Binding CsSeries}"
                                        MinHeight="160"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
